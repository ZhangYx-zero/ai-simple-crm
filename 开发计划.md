# AI-CRM 简历项目 · 开发计划（同时是readme文件,自己动手写）

> 这份文档是给你**自己写代码**用的，不是让你抄。目标：做出一个**能跑、能讲、能写进简历**的"简易版 AI 销售 CRM"。
> 参考对象：Cordys CRM（飞致云开源 AI CRM）——你只参考它的**业务形态**（线索→客户→商机→跟进 + AI 助手），代码完全自己写。
> 前置基础：Java 17、Maven、SpringBoot（课件 08）、MyBatis（课件 07）、MySQL、Redis。

---

## 1. 项目定位（先定简历标题）

- **简历项目名**：AI 智能销售 CRM 系统（Smart Sales CRM）
- **一句话定位**：一个"以客户为中心的销售过程管理"后端系统，并接入大模型实现"AI 销售助手"，能够基于客户/商机数据回答销售问题。
- **你做它的意义**：它不是普通增删改查，同时覆盖了**真实业务建模（CRM）+ 数据权限 + AI 集成**三个面试高频点，比"又一个管理系统"有辨识度。

> 学习素材搭配：先看课件里我写的《CRM学习笔记-销售管理型CRM.md》理解业务；再看本计划动手；写完后对照 Cordys CRM 的界面/模块名字，能说出"我做了它的哪一部分、为什么那样做"即可。

---

## 2. 技术栈与版本

| 层 | 选型 | 说明 |
| --- | --- | --- |
| 语言 | Java 17 | |
| 框架 | Spring Boot 3.x | 3.2+ 均可 |
| 持久层 | MyBatis-Plus 3.5.x | `mybatis-plus-spring-boot3-starter` |
| 数据库 | MySQL 8.0 | 库名 `ai_crm` |
| 缓存/会话 | Redis | 存 Sa-Token 会话 |
| 认证 | Sa-Token（+可选 JWT 模式） | `sa-token-spring-boot3-starter` |
| API 文档 | Knife4j(OpenAPI3) | 访问 `/doc.html` |
| AI 调用 | 大模型 HTTP API（OpenAI 兼容） | 推荐 DeepSeek/通义/智谱；无 Key 时用 Mock 模式 |
| JSON | Jackson（Spring 自带） | |
| 工具 | Hutool（可选） | 简化 HTTP/断言，不强求 |

> 关键依赖（pom）：
> ```xml
> spring-boot-starter-web
> com.baomidou:mybatis-plus-spring-boot3-starter:3.5.7
> com.mysql:mysql-connector-j
> cn.dev33:sa-token-spring-boot3-starter:1.39.0
> cn.dev33:sa-token-redis-jackson:1.39.0     <!-- Sa-Token 会话存 Redis -->
> org.springframework.boot:spring-boot-starter-data-redis
> com.github.xiaoymin:knife4j-openapi3-jakarta-spring-boot-starter:4.5.0
> lombok
> ```

---

## 3. 功能范围（MVP）

### 3.1 要做（MVP）
1. **登录认证**：用户名密码登录、登出、获取当前登录人；Sa-Token + Redis。
2. **用户与角色**：预置 3 个角色：`ADMIN`(管理员/看全部)、`MANAGER`(主管/看全部)、`SALES`(销售/只看自己名下)。登录返回角色。
3. **线索管理**：线索 CRUD + 分页搜索 + **线索转客户**（事务）。
4. **客户管理**：客户 CRUD + 分页搜索 + **数据权限**（SALES 只能看 `owner_id=自己`）+ 客户归属转移。
5. **联系人管理**：一个客户多个联系人，作为客户下的子资源 CRUD。
6. **商机管理**：商机 CRUD + 按客户关联 + **阶段推进**（含状态机校验，赢/输单）。
7. **跟进记录**：给客户/商机追加跟进，形成时间线。
8. **AI 销售助手**：销售选中某客户/商机提问，系统把**当前人有权限看到**的数据整理成上下文，调用大模型回答，并保存问答记录。
9. **销售漏斗统计**：一个接口按商机阶段聚合数量与金额（给 AI 助手和将来的图表用）。

### 3.2 明确不做（控制范围，做完再谈）
- ❌ 不做前端页面（用 Knife4j + Postman 验证）
- ❌ 不做审批流、自定义字段/动态表单、Excel 导入（这些是 Cordys 的复杂度，别掉进去）
- ❌ 不做多租户（只做"用户+数据权限"）
- ❌ 不自己训练/部署模型（只**调用**大模型 API）

### 3.3 功能 → 模块划分
| 模块 | 对应功能 | 后端包 |
| --- | --- | --- |
| auth | 登录/登出/当前用户 | `auth` |
| user | 用户、角色 | `user` |
| lead | 线索、转客户 | `lead` |
| customer | 客户、联系人 | `customer`（含 `contact`） |
| opportunity | 商机、阶段推进 | `opportunity` |
| follow | 跟进记录 | `follow` |
| dashboard | 漏斗统计 | `dashboard` |
| ai | AI 销售助手 | `ai` |

---

## 4. 工程结构（后端单模块即可，别拆多模块）

```
ai-crm/
├─ pom.xml
├─ src/main/java/com/xxx/crm/
│  ├─ CrmApplication.java
│  ├─ common/            # 统一返回 Result、全局异常、分页、常量、当前用户工具
│  ├─ config/            # Sa-Token、MyBatis-Plus、Knife4j、Redis 配置
│  ├─ security/          # Sa-Token 拦截/权限、登录处理器（如需要）
│  ├─ entity/            # 表实体（@TableName）
│  ├─ mapper/            # BaseMapper 子接口
│  ├─ service/           # 接口 + impl（业务规则放这里）
│  ├─ controller/        # REST 接口（薄）
│  └─ ai/                # AI 客户端封装（接口 + 实现 + Mock）
└─ src/main/resources/
   ├─ application.yml
   └─ mapper/*.xml（可选，复杂 SQL 放这）
```

> 依赖方向参照你学过的规范：`controller → service → mapper`；`entity` 不往上漏；业务规则/事务/数据权限写在 `service`。

---

## 5. 数据库设计（MySQL，库名 `ai_crm`）

### 5.1 表关系
```
sys_user(用户/销售) 1──拥有(owner_id)──> crm_customer
crm_customer 1──< crm_contact(联系人)
             └──< crm_opportunity(商机)
crm_opportunity 1──< crm_follow_up(跟进)
crm_lead(线索) 转正 ──> 生成 crm_customer
crm_ai_chat_log(AI 问答留痕，独立)
```

### 5.2 建表 SQL

```sql
CREATE DATABASE IF NOT EXISTS ai_crm DEFAULT CHARACTER SET utf8mb4;
USE ai_crm;

-- 用户表（含角色，简单起见不分权限表；真项目才拆 RBAC）
CREATE TABLE sys_user (
    id          BIGINT PRIMARY KEY AUTO_INCREMENT,
    username    VARCHAR(50)  NOT NULL,
    password    VARCHAR(100) NOT NULL COMMENT 'BCrypt 加密',
    nickname    VARCHAR(50),
    role        VARCHAR(20)  NOT NULL DEFAULT 'SALES' COMMENT 'ADMIN/MANAGER/SALES',
    status      TINYINT      NOT NULL DEFAULT 1 COMMENT '1启用 0停用',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY uk_username (username)
) COMMENT='用户';

-- 线索
CREATE TABLE crm_lead (
    id          BIGINT PRIMARY KEY AUTO_INCREMENT,
    name        VARCHAR(50)  NOT NULL COMMENT '线索名称',
    company     VARCHAR(100) COMMENT '公司',
    phone       VARCHAR(20),
    source      VARCHAR(20) COMMENT '来源:展会/网络/转介绍/外呼',
    status      TINYINT NOT NULL DEFAULT 0 COMMENT '0新 1跟进中 2已转客户 3已关闭',
    owner_id    BIGINT COMMENT '当前跟进销售',
    remark      VARCHAR(500),
    deleted     TINYINT NOT NULL DEFAULT 0,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    KEY idx_owner (owner_id)
) COMMENT='销售线索';

-- 客户
CREATE TABLE crm_customer (
    id          BIGINT PRIMARY KEY AUTO_INCREMENT,
    name        VARCHAR(100) NOT NULL,
    industry    VARCHAR(50) COMMENT '行业',
    level       VARCHAR(10)  COMMENT 'A/B/C',
    source      VARCHAR(20),
    owner_id    BIGINT COMMENT '归属销售(数据权限)',
    status      TINYINT NOT NULL DEFAULT 0 COMMENT '0潜在 1正式 2流失',
    phone       VARCHAR(20),
    address     VARCHAR(200),
    remark      VARCHAR(500),
    deleted     TINYINT NOT NULL DEFAULT 0,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    KEY idx_owner (owner_id)
) COMMENT='客户';

-- 联系人
CREATE TABLE crm_contact (
    id          BIGINT PRIMARY KEY AUTO_INCREMENT,
    customer_id BIGINT NOT NULL,
    name        VARCHAR(50) NOT NULL,
    position    VARCHAR(50) COMMENT '职位',
    phone       VARCHAR(20),
    wechat      VARCHAR(50),
    is_decision TINYINT NOT NULL DEFAULT 0 COMMENT '是否决策人',
    remark      VARCHAR(500),
    deleted     TINYINT NOT NULL DEFAULT 0,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    KEY idx_customer (customer_id)
) COMMENT='联系人';

-- 商机
CREATE TABLE crm_opportunity (
    id           BIGINT PRIMARY KEY AUTO_INCREMENT,
    name         VARCHAR(100) NOT NULL COMMENT '商机名称',
    customer_id  BIGINT NOT NULL,
    owner_id     BIGINT COMMENT '跟进销售',
    amount       DECIMAL(12,2) NOT NULL DEFAULT 0 COMMENT '预计金额(元)',
    stage        TINYINT NOT NULL DEFAULT 1 COMMENT '1初步接触 2需求确认 3方案报价 4商务谈判 5赢单 6输单',
    expect_date  DATE,
    win_rate     INT NOT NULL DEFAULT 10 COMMENT '赢单率%',
    fail_reason  VARCHAR(200) COMMENT '输单原因',
    deleted      TINYINT NOT NULL DEFAULT 0,
    create_time  DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time  DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    KEY idx_customer (customer_id),
    KEY idx_owner (owner_id)
) COMMENT='商机';

-- 跟进记录
CREATE TABLE crm_follow_up (
    id             BIGINT PRIMARY KEY AUTO_INCREMENT,
    customer_id    BIGINT COMMENT '关联客户',
    opportunity_id BIGINT COMMENT '关联商机',
    type           VARCHAR(20) COMMENT '电话/见面/微信/邮件/系统',
    content        VARCHAR(1000) COMMENT '跟进内容',
    next_time      DATETIME COMMENT '下次跟进',
    create_by      BIGINT COMMENT '记录人',
    create_time    DATETIME DEFAULT CURRENT_TIMESTAMP,
    deleted        TINYINT NOT NULL DEFAULT 0,
    KEY idx_opportunity (opportunity_id)
) COMMENT='跟进记录';

-- AI 问答留痕
CREATE TABLE crm_ai_chat_log (
    id             BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id        BIGINT NOT NULL,
    customer_id    BIGINT COMMENT '上下文客户(可空)',
    opportunity_id BIGINT COMMENT '上下文商机(可空)',
    question       VARCHAR(1000) NOT NULL,
    answer         TEXT,
    model          VARCHAR(50),
    create_time    DATETIME DEFAULT CURRENT_TIMESTAMP
) COMMENT='AI助手问答日志';

-- 演示数据（密码请用 BCrypt 生成后替换）
INSERT INTO sys_user (username, password, nickname, role) VALUES
('admin', '$2a$10$你生成的hash', '管理员', 'ADMIN'),
('seller', '$2a$10$你生成的hash', '销售小王', 'SALES');
```

### 5.3 设计要点（写代码前想清楚）
- **金额**：`DECIMAL(12,2)` + Java `BigDecimal`，禁止 double。
- **逻辑删除**：`deleted` 字段 + MyBatis-Plus `@TableLogic`，删除变更新。
- **数据权限**：核心表都有 `owner_id`；"销售看自己、主管/管理员看全部"在 service 统一处理。
- **状态**：`TINYINT` + Java 常量/枚举；状态流转校验放 service（见商机阶段）。
- **审计字段**：`create_time/update_time` 交给 MyBatis-Plus 自动填充，别手写。

---

## 6. 接口设计（REST 清单）

统一前缀 `/api`，统一返回 `{code,message,data}`，鉴权用请求头 `Authorization: <token>`。

| 方法 | 路径 | 说明 | 权限 |
| --- | --- | --- | --- |
| POST | /api/auth/login | 登录，返回 token + 用户信息 | 公开 |
| POST | /api/auth/logout | 登出 | 登录 |
| GET | /api/auth/me | 当前登录人 | 登录 |
| GET | /api/customers/page | 客户分页(名称/行业/归属筛选) | 登录+数据权限 |
| GET | /api/customers/{id} | 客户详情(含联系人) | 数据权限 |
| POST | /api/customers | 新增客户 | 登录 |
| PUT | /api/customers/{id} | 修改客户 | 数据权限 |
| DELETE | /api/customers/{id} | 删除(逻辑) | 数据权限 |
| PUT | /api/customers/{id}/transfer | 转移归属(ownerId) | MANAGER/ADMIN |
| GET | /api/customers/{customerId}/contacts | 客户联系人列表 | 数据权限 |
| POST | /api/customers/{customerId}/contacts | 新增联系人 | 数据权限 |
| GET | /api/leads/page | 线索分页 | 登录 |
| POST | /api/leads/{id}/convert | 线索转客户(事务) | 登录 |
| GET | /api/opportunities/page | 商机分页(可按客户/阶段) | 数据权限 |
| POST | /api/opportunities | 新增商机 | 登录 |
| PUT | /api/opportunities/{id}/stage | 商机阶段推进 | 数据权限 |
| GET | /api/opportunities/{id}/follow-ups | 商机跟进时间线 | 数据权限 |
| POST | /api/follow-ups | 追加跟进记录 | 登录 |
| GET | /api/dashboard/funnel | 销售漏斗(按阶段聚合) | 数据权限 |
| POST | /api/ai/chat | AI 销售助手问答 | 登录+数据权限 |
| GET | /api/ai/chats | 我最近的 AI 问答记录 | 登录 |

> 每个接口做完，用 Knife4j(`/doc.html`) 或 Postman 实测，文档里给了 curl 示例可自测。

---

## 7. AI 销售助手 设计（重点，简历亮点）

### 7.1 思路
不是"在系统里内嵌一个模型"，而是：
1. 用户提问时，后端把**当前登录人有权限看到的客户/商机/跟进数据**取出来；
2. 拼成一个**结构化 prompt**（系统人设 + 数据上下文 + 用户问题）；
3. 调用**大模型 HTTP API**（OpenAI 兼容格式）拿到回答；
4. 把问答存 `crm_ai_chat_log`，返回给调用方。

### 7.2 请求流程
```
销售提问(question, 可选 customerId/opportunityId)
  → 校验该客户/商机归属权限（防越权）
  → 组装上下文：客户档案 + 关联商机 + 最近跟进记录
  → 调用 AiClient.chat(messages)
  → 落库 crm_ai_chat_log
  → 返回 answer
```

### 7.3 关键设计点
- **AiClient 做成接口**，两个实现：
  - `OpenAiCompatClient`：真实调用大模型（OpenAI 兼容 `/chat/completions`）；
  - `MockAiClient`：`ai.enabled=false` 时返回"我是模拟助手…"（用于没 API Key / 联调演示）。
  - 用 `@ConditionalOnProperty` 切换，简历可写"封装统一 AI 客户端接口，支持 Mock/真实模型切换"。
- **配置外置**（`application.yml` + 环境变量，别把 key 写死在代码）：
  ```yaml
  ai:
    enabled: true
    base-url: https://api.deepseek.com/v1   # OpenAI 兼容地址
    api-key: ${AI_API_KEY:}
    model: deepseek-chat
    timeout-seconds: 60
  ```
- **上下文权限**：AI 只能看到"当前用户有权访问"的数据；客户/商机归属校验复用 service 层方法。
- **Prompt 模板**：给一个明确人设，例如：
  > 你是资深销售教练。以下是销售 {nickname} 的客户与商机资料：…（结构化文本）。请基于这些信息回答：{question}
- **超时/失败兜底**：调用设置超时；失败时返回友好错误而不是 500 堆栈。
- 发请求用 JDK 自带 `java.net.http.HttpClient` + Jackson 即可（不引额外依赖），请求体示例：
  ```json
  { "model": "deepseek-chat",
    "messages": [ {"role":"system","content":"你是销售教练"}, {"role":"user","content":"..."} ] }
  ```
  响应里取 `choices[0].message.content`。

### 7.4 简历里的"AI"描述口径
> "接入 OpenAI 兼容大模型 API，基于数据权限封装 CRM 上下文，实现 AI 销售助手（客户画像问答/跟进建议）；设计 Mock 实现便于无 Key 联调。"

---

## 8. 分步开发计划（按顺序做，每步可验证）

> 建议每天 2~4 小时，约 1~2 周。**每完成一步都先跑通再进入下一步**。

### Step 0：初始化工程
- 建 Spring Boot 工程（可 start.spring.io），引入第 2 节依赖；`application.yml` 配好数据源/Redis。
- 建库建表（执行第 5 节 SQL），插入演示用户。
- 写 `Result<T>`、全局异常处理器、分页 `PageResult`。
- ✅ 验证：项目能启动，访问 `/doc.html` 打开 Knife4j。

### Step 1：登录认证（Sa-Token + Redis）
- 配 Sa-Token（token 存 Redis），`SaTokenConfig` 放行 `/api/auth/login`、`/doc.html`、`/v3/api-docs/**`。
- 密码用 BCrypt（`spring-security-crypto` 只引 crypto 即可）校验。
- 登录成功后 `StpUtil.login(userId)`，自定义返回 token + 用户(含 role)。
- 写 `CurrentUser` 工具（从 `StpUtil.getLoginId()` 取当前用户 id/role）。
- ✅ 验证：curl 登录拿到 token，带 token 访问 `/api/auth/me` 返回自己；Redis 里能看到会话 key。

### Step 2：客户模块 + 数据权限（核心，先做这个）
- `entity/mapper/service/controller` 四件套完成客户 CRUD + 分页搜索。
- 数据权限：service 查询时若 `role=SALES` 则强制 `owner_id = 当前用户`；`MANAGER/ADMIN` 可传 `ownerId` 筛选。
- 写"新增/修改/删除/详情"的越权校验：SALES 只能改删自己的客户。
- ✅ 验证：用 `seller` 登录只能查到/改到自己的客户；`admin` 能查全部、能转移归属。

### Step 3：线索模块 + 线索转客户
- 线索 CRUD + 分页。
- `convert` 方法：`@Transactional`，校验线索状态未转 → 建客户 + 建联系人 + 改线索状态为已转。
- ✅ 验证：转换后客户表多一条、线索状态变 2；重复转换被拦截（事务 + 状态校验）。

### Step 4：商机模块 + 跟进记录
- 商机 CRUD，归属销售默认 = 当前用户。
- 阶段推进接口：校验"赢/输单不能再改、不能回退(除输单)"，推进同时插入一条跟进记录（`type=系统`）。
- 跟进记录：列表按时间倒序 = 时间线。
- ✅ 验证：把一个商机从 1 推到 5，再推 6 被拒；时间线能看到每次系统记录。

### Step 5：AI 销售助手（简历亮点，放最后是因为它依赖前面数据）
- 建 `ai` 包：`AiClient` 接口 + `OpenAiCompatClient` + `MockAiClient` + `AiChatService` + `AiChatController`。
- `chat` 流程按 7.2 实现；上下文组装复用"客户+商机+跟进"的查询（同样要数据权限）。
- 保存 `crm_ai_chat_log`；无 Key 时 `ai.enabled=false` 走 Mock。
- ✅ 验证：有 Key 用真实模型问"这个客户适合推什么产品/下一步怎么跟进"有合理回答；无 Key 用 Mock 也能返回；越权问别人的客户被拒。

### Step 6：漏斗统计 + 收尾
- `GET /api/dashboard/funnel`：MyBatis-Plus 或自定义 SQL 按 `stage` 聚合 `count(*)/sum(amount)`，同样带数据权限。
- 补一两条自定义 Mapper SQL（如漏斗）放 `mapper/*.xml`，证明你会写 SQL。
- ✅ 验证：造几个不同阶段商机，漏斗数据正确。

### Step 7：打磨 + 写简历
- 统一异常/参数校验补全；补 2~3 个核心 Service 的单元测试（可选但加分）。
- 整理 README：项目简介、技术栈、表结构、启动方式、接口列表、演示截图(curl 结果)。
- 用第 9 节模板写简历描述。

---

## 9. 简历怎么写

### 一句话
> AI 智能销售 CRM 系统（Spring Boot 3 + MyBatis-Plus + MySQL + Redis + Sa-Token + 大模型 API）：实现"线索→客户→商机→跟进"销售全流程管理与数据权限控制，并接入大模型实现 AI 销售助手，支持基于客户数据的智能问答与跟进建议。

### 亮点条目（3 条）
1. **业务闭环 + 状态机**：线索转客户、商机阶段推进（赢/输单）等关键流转用事务与状态校验保证数据一致性；
2. **数据权限设计**：基于 `owner_id` + 角色在 Service 层统一控制"销售只看自己/主管看全部"，杜绝越权访问；
3. **AI 集成**：封装统一 AI 客户端（真实大模型/Mock 可切换），按数据权限组装 CRM 上下文实现销售助手问答，并落库留痕。

### 面试可能被问
- 数据权限怎么实现？为什么要放 service 不放 controller？
- 线索转客户为什么加事务？
- 商机阶段状态机怎么设计的？不允许回退怎么校验？
- AI 上下文怎么保证不越权？如果模型乱答怎么办？
- Sa-Token 的 token 存哪里？和 JWT 有什么区别？
- 金额用什么类型？为什么？

---

## 10. 风险与时间
- **最大风险：范围失控**。务必只做 MVP（第 3.1），"不做清单"(3.2) 是保护你的。
- **AI Key/费用**：没有 Key 就用 Mock 模式；有 Key 用 DeepSeek 等便宜模型，注意把 key 放环境变量别提交。
- **不要抄 Cordys**：参考业务形态即可；代码全自己写，才能讲得清、才是你的简历作品。
- 若某步卡住 >2 小时，先记录问题去问/查，不要硬耗。

---

## 附 A：curl 自测参考
```bash
# 登录
curl -X POST http://localhost:8080/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"你的密码"}'

# 带 token 查客户分页（把 <token> 换成登录返回值）
curl "http://localhost:8080/api/customers/page?pageNum=1&pageSize=10" \
  -H "Authorization: <token>"

# AI 助手提问
curl -X POST http://localhost:8080/api/ai/chat \
  -H "Content-Type: application/json" -H "Authorization: <token>" \
  -d '{"customerId":1,"question":"这个客户怎么跟进？"}'
```

## 附 B：常见坑
1. Sa-Token 登录后 401 → 检查 Redis 通不通、放行路径配没配。
2. MyBatis-Plus 逻辑删除 → 实体加 `@TableLogic`，全局配置 `logic-delete-field: deleted`。
3. 金额查询用 BigDecimal，JSON 序列化注意保留两位。
4. AI 请求要设超时，别让接口卡几十秒。
5. 密码别明文入库，BCrypt 加密。
